<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Gm1KonverterCrossPlatform.ViewModels;assembly=Gm1KonverterCrossPlatform"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="600"
        Width="1200"
        Height="600"
        x:Class="Gm1KonverterCrossPlatform.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        
        Title="Gm1KonverterCrossPlatform">

  <Design.DataContext>
    <vm:MainWindowViewModel/>
  </Design.DataContext>


  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="20"/>
      <RowDefinition Height="550"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="250"/>
      <ColumnDefinition Width="700"/>
      <ColumnDefinition Width="250"/>
    </Grid.ColumnDefinitions>
    <Menu DockPanel.Dock="Top" Grid.Row="0" Grid.ColumnSpan="3" >
      <MenuItem Header="_File">
        <MenuItem Header="_Create new GM1" Name="CreatenewGM1MenueItem" IsEnabled="{Binding ButtonsEnabled}"/>
        <Separator/>
        <MenuItem Header="_Replace with Saved GM1 File" Name="ReplacewithSavedGM1FileMenueItem" IsEnabled="{Binding ReplaceWithSaveFile}"/>
    
      </MenuItem>
      <MenuItem Header="_Edit">
        <MenuItem Header="Export Colortable" Name="ExportColortableMenueItem" IsEnabled="{Binding ColorButtonsEnabled}"/>
        <MenuItem Header="Import Colortable" Name="ImportColortableMenueItem" IsEnabled="{Binding ColorButtonsEnabled}"/>
        <Separator/>
        <MenuItem Header="Export Images" Name="ExportImagesMenueItem" IsEnabled="{Binding ButtonsEnabled}"/>
        <MenuItem Header="Import Images" Name="ImportImagesMenueItem" IsEnabled="{Binding ImportButtonEnabled}"/>
      </MenuItem>
      <MenuItem Header="_Options">
        <MenuItem Header="_Workfolder" Name="WorkfolderMenueItem"/>
        <Separator/>
        <MenuItem Header="_Crusader GM1 Folder" Name="CrusaderfolderMenueItem"/>
        <Separator/>
        <CheckBox Content="Open Folder after Export" Name="UserConfigOpenFolderafterExport" IsChecked="{Binding OpenFolderAfterExport}"/>
        <CheckBox Content="Activate Logger" IsChecked="{Binding LoggerActiv}"/>
      </MenuItem>
    </Menu>
    <StackPanel Grid.Row="1" Grid.Column="0" MaxWidth="250">
      <TextBlock Text="Work Folder:" HorizontalAlignment="Left" Height="20"/>
      <ListBox Items="{Binding WorkfolderFiles}" SelectionMode="Single" Name="WorkfolderSelector" MaxWidth="250" MaxHeight="560"/>
    </StackPanel>
    
    <StackPanel Grid.Row="1" Grid.Column="2" MaxWidth="250">
      <TextBlock Text="Stronghold Folder:" HorizontalAlignment="Left" Height="20"/>
      <ListBox Items="{Binding StrongholdFiles}" SelectionMode="Single" Name="Gm1FilesSelector" MaxWidth="250" MaxHeight="560"/>
    </StackPanel>


    <StackPanel Grid.Column="1" Grid.Row="2">
      <TextBlock Text="{Binding Filetype}" HorizontalAlignment="Center" Height="20"/>
    
      <StackPanel Orientation="Vertical">
        <TextBlock Text="{Binding ActualPalette}" IsVisible="{Binding ColorButtonsEnabled}" HorizontalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
          <Button IsEnabled="{Binding ColorButtonsEnabled}" Content="««" Width="40" Click="Button_ClickPalleteminus" IsVisible="{Binding ColorButtonsEnabled}" HorizontalAlignment="Center"/>
          <Image Source="{Binding ActuellColorTable}" IsVisible="{Binding ColorButtonsEnabled}" HorizontalAlignment="Center"/>
          <Button IsEnabled="{Binding ColorButtonsEnabled}" Content="»»" Width="40" Click="Button_ClickPalleteplus" IsVisible="{Binding ColorButtonsEnabled}" HorizontalAlignment="Center"/>

          <Button IsEnabled="{Binding ColorButtonsEnabled}" Click="Button_ClickChangeColorTable" IsVisible="{Binding ColorButtonsEnabled}" HorizontalAlignment="Right">Change Color Table</Button>
        </StackPanel>
      </StackPanel>



      <ListBox Items="{Binding TGXImages}" MaxHeight="400" Name="TGXImageListBox" MaxWidth="700">
        <ListBox.ItemsPanel>
          <ItemsPanelTemplate>
            <WrapPanel Orientation="Horizontal" MaxWidth="650"/>
          </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
      </ListBox>
    </StackPanel>
  </Grid>









</Window>
